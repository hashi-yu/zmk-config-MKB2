description: Time-independent Naginata space and layer behavior

compatible: "zmk,behavior-naginata-space"

include: two_param.yaml

properties:
  bindings:
    type: phandles
    required: true
  tapping-term-ms:
    type: int
    required: true
  shortcut-binding:
    type: phandle
    required: true
    description: Raw key behavior used when an HRM hold is already active
  tap-trigger-key-positions:
    type: array
    required: true
    description: Opposite-side positions that convert the held layer into Naginata Space
